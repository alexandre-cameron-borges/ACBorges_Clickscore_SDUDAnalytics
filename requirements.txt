# — Traitement des données —
pandas>=1.5.0
numpy>=1.24.0

# — Machine Learning classique —
scikit-learn>=1.2.0
lightgbm>=4.0.0
optuna>=3.0.0

# — Traitement du texte / NLP —
transformers>=4.30.0
torch>=2.0.0             # backend BERT

# — Visualisation & Notebook —
jupyterlab>=4.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# — Développement & CI —
nbstripout>=0.5.0
jupytext>=1.14.0

# — WebApp & tunneling —
streamlit>=1.25.0
pyngrok>=5.2.1

# — Intégration Kaggle —
kaggle>=1.5.0
